using UnityEngine;

public struct LaserTargetPositionSet
{
    public readonly Vector3 TargetPosition;

    public LaserTargetPositionSet(Vector3 targetPosition)
    {
        TargetPosition = targetPosition;
    }
}